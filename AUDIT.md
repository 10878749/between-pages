# 互动改版验证

2026-09-20。新版主流程：ClueScene → ParcelScene → ReadingScene。

- 移除手动搜索/导入入口、广告文案及长页模块。
- 外部候选自动获取，中文版本筛选，缓存记录允许空的宣传性旧字段。
- Open Library 随机查询；Google Books 备用；无缓存断网明确重试，避免静默换成本地书。
- 390×844、768×1024、1440×900 无页面溢出。详情在书内滚动。
- 保留按钮和键盘替代、触摸取消回弹、开盒锁、原生 dialog 焦点管理及减少动画设置。
- 当前 reading 场景 axe WCAG A/AA 扫描零问题。自动检测不替代全面人工无障碍评估。
- 封面为原创排印；外部简介和中文版本信息保留来源，不编造内容。

本次只更新本地项目，未发布。

## 酒红牌阵迭代

- 主题牌阵每手最多六张，覆盖心情、想得到什么、MBTI、氛围、类型与阅读状态；主题切换保留选择，同主题可翻手。
- 酒红背景与石灰白纸面，静态纸边、低对比装帧线、选中标记；仅鼠标启用悬停层级，避免触摸误点相邻牌。
- 书本捕获指针并随手移动；触摸阈值 45px，鼠标 60px；取消回弹，拦截滑动结束后误触点击。
- tests/fan.mjs 通过：真实浏览器触摸事件、卡牌选择、MBTI 分页、跨主题选择保留、书本取消/左滑/合上/右滑与三尺寸截图。
- 初始牌阵和稳定阅读画面的 axe 扫描通过。场景过渡期间的透明度不作为稳定画面对比度测量结果。
- AI 内容尚未启用；AI-DETAILS-PLAN.md 提供服务端检索与生成方案。

## 时段机会与模型接口

- 本地 Node API、签名访客 Cookie、八小时窗口两次抽取；服务器预留/提交/退回、UUID 幂等、刷新恢复。
- 每日 100 次生成尝试、单并发、每本每次抽取每日最多三次尝试，手动重试至少间隔 60 秒。
- 缓存通用书籍详情和标签组合推荐理由。缺资料时强制不输出简介/作者经历。尚未配置真实智谱密钥。
- 单元/接口测试覆盖边界时间、持久化、并发、请求重放、第三次拒绝、失败退回、身份隔离、缓存、生成上限和固定免费模型。

## 2026-09-20 推敲与随手模式联调

- 64 项自动测试通过；ESLint、TypeScript 与生产构建通过。
- `tests/upgrade-flow.mjs`：Edge 移动触摸事件验证整条封带中央右移 28px 触发、取消/垂直滑动不触发；验证轻量不扣次数、主动升级扣一次、翻开后返回选牌与继续阅读。360×640、390×844、1440×900 布局检查通过，书页与次数提示不重叠。
- 普通 5173 与私用 5174 都实际完成外部书库轻量抽取，未消耗推敲机会。私用口令、独立预算与隐藏状态文件检查通过。
- 实际免费生成服务返回 HTTP 429 / 1305（繁忙），推敲选书失败已退款；未取得这轮真实生成详情的成功样本。排名与详情的成功路径目前由受控接口和单元测试验证，不宣称已完成真实跨书目质量验收。
- 运行说明与限制已更新在 SERVER.md。

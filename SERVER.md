# 页间：运行与测试

## 两个入口

```powershell
# 普通版：每 8 小时 5 次推敲机会
npm run dev -- --host 0.0.0.0 --port 5173 --strictPort

# 私用版：独立数据、不限应用内推敲次数，需口令
npm run dev:play
```

手机与电脑使用同一 Wi-Fi，打开终端显示的 Network 地址。当前普通版为 `http://192.168.71.66:5173/`，私用版为 `http://192.168.71.66:5174/`；电脑 IP 改变时以终端为准。

私用版首次运行会生成 `.local-data/play-access.txt`。在本机打开这个文件，复制口令到私用版页面即可。口令不出现在网页源码、URL 或日志里；修改文件并重启私用服务可使旧访问凭据失效。私用与普通版使用不同的签名 Cookie、记录文件，普通版不能通过 URL 参数取消限额。

服务端 API 由 Vite 插件提供。`npm run build` 只构建前端，生产预览需使用 `npx vite preview --host 0.0.0.0`；单独上传 dist 不会提供 API。不要让多个进程共享同一个状态文件。

## 接入

`.env.local` 中配置 `BIGMODEL_API_KEY`，只请求 `glm-4.7-flash`，没有付费回退。密钥不要添加 VITE_ 前缀。供应商免费政策、并发与可用性以其平台为准；私用无限次数不代表可以绕过供应商限流。

当前机器外部书库通过 `LIBRARY_PROXY_URL=http://127.0.0.1:7993` 访问，生成接口直接连接。更换网络后根据实际情况修改代理配置。不要提交 `.env.local`、`.local-data`。

## 使用规则

- **推敲选书**：检索外部书库，过滤明显不合适的专业书与最近抽过的书，取最多 8 本候选、补取资料，再由生成服务比较。验证候选 ID、证据片段、线索与分数后，在较合适的候选间加权抽取；约 15% 的探索仍受适宜性过滤约束。
- **随手抽书**：仅用程序筛选和抽取，全程不调用生成服务，不消耗推敲机会。缺失简介时显示稳定于本次抽取的简短文案。
- **推敲这本书**：随手抽到后可主动使用 1 次机会补介绍；保留原书。成功并通过质量检查才扣次，失败释放预留。
- 推敲选书成功使用 1 次机会，已包含这本书的详情补全。详情失败重试不另扣机会。
- 已解锁书籍可再次查看；其他访客首次解锁相同的缓存内容仍需 1 次机会。
- 北京时间 00:00、08:00、16:00 恢复 5 次，不累积；随手抽书不受此限制。
- 访客通过 HttpOnly 签名 Cookie 标识。刷新、多标签共享记录；清除 Cookie 或更换设备会成为新访客。这是匿名测试身份，并非登录账号。
- 预留、完成与退款持久化；同一抽取 UUID 幂等；重启释放中断的预留。普通版 `.local-data/state.json`，私用版 `.local-data/play-state.json`。
- “返回选牌”不扣机会，保留线索和当前书。可点“继续看这本书”回来。

## 内容与可靠性

资料来自 Open Library 作品及其关联作者；简介缺失时查找标题与作者严格匹配的 Google Books 记录；原有编辑资料只有书名及作者匹配才复用。避免仅按姓名合并作者。资料不足时不编造简介、奖项或生平。

固定提示词包含原有详情的三段文风样例，要求“书的内容、作者、与线索的关系、读法”。服务器检查长度、必需字段、引用片段能否在资料找到、线索是否出现及明显广告措辞。此类检查能过滤常见问题，不能证明每句话完全符合资料，仍需内容抽检。

缓存按 `editorial-v3` 分版本，通用介绍与个性化关系分开；缺失作者资料不写入完整通用缓存，旧版空白缓存不会被当成已完成。阅读页先显示书目，再异步补全介绍；再次进入不会自动反复重试失败请求。

每个实例单并发生成；可重试的网络/429/5xx最多重试一次，计入实际调用预算。遵守最长 10 秒的 Retry-After，较长等待交回用户；繁忙后短暂冷却。普通版不设全站每日 Token 或调用总量上限，同一抽取每天最多 3 次详情尝试。私用版取消应用内次数上限，保留单并发与短暂重试间隔。

## 验证

```powershell
npm test
npm run lint
npm run build
node tests/upgrade-flow.mjs
```

自动测试覆盖排名拒绝伪造 ID/证据、20 种详情质量失败、预算/退款/缓存/访客隔离、私用口令。浏览器测试使用受控 API 验证真实触摸事件、封带中央短滑、取消与垂直滑动、返回选牌、轻量补全以及手机和桌面布局，不消耗真实生成机会。

旧 `quota.mjs` 等脚本针对旧流程保留作参考，不作为当前版本验收命令。

交互顺序：确认纸签 → 自动找书（封带与拆开按钮锁定）→ 书已备好 → 用户右拉或点击拆开。找书成功沿用已有计次规则；拆开不再请求书库或扣次。当前页面内返回选牌、不改线索再回来会复用已准备的书。

## 响应式布局与拆封状态（2026-09-20）

页面按导航、互动内容、机会次数、底部进度依次排布，使用动态视口与安全区间距。短屏缩小包裹并允许文档纵向滚动，不再把所有内容压入固定高度。机会区与操作区保留至少 32px 间距；阅读页返回按钮占独立一行。

找书时封条锁定且没有箭头，不显示拆开按钮；找到后显示“书已备好”、右拉提示和拆开按钮，封条右端箭头轻移两次后停止。减少动态效果设置下直接呈现。

找书期间可返回选牌，后台任务继续；不会在完成时自动跳页。页首显示“查看找书进度”或“书已备好”，可回到原任务。

浏览器回归覆盖 320×568、360×640、390×844、430×932、844×390，以及放大文字；检查包裹不遮挡成功提示、拆开按钮与次数区间距和横向溢出。结果基于浏览器模拟，尚不等同所有实体设备实测。

## 百炼接入（当前选择）

当前本地已选择 `MODEL_PROVIDER=bailian`，依次调用北京地域 `qwen-flash` 与 `qwen-flash-2025-07-28`。在 https://bailian.console.aliyun.com/ 注册或登录，选择华北2（北京），创建 API Key，并为 qwen-flash 开启“免费额度用完即停”。创建密钥说明：https://help.aliyun.com/zh/model-studio/get-api-key 。免费额度说明：https://help.aliyun.com/zh/model-studio/new-free-quota 。

在 `.env.local` 填入 `DASHSCOPE_API_KEY`，完成平台开关设置后将 `BAILIAN_FREE_QUOTA_CONFIRMED=true`。本地标记仅记录用户确认，无法读取或保证云端开关状态；真正的免费用完停止由百炼控制台执行。未确认时应用和诊断脚本均不发送百炼请求。不要把百炼密钥填进 BIGMODEL_API_KEY。

重启两个服务，再运行 `npm run diagnose` 做单次最小请求；脚本不会输出密钥。没有配置密钥时无法完成真实调用测试。额度耗尽不会自动切换收费服务；随手抽书仍无生成调用。切回智谱可设置 `MODEL_PROVIDER=bigmodel`。


## 2026-09-20 书目质量修复

外部召回改为 Open Library 中文版本、至少三版、按版本数量排序；每次合并首页100条和后续一页100条，Google Books作为无结果时的备用。版本数量仅作为初筛信号，不作为评分或质量保证。当前未接入豆瓣，也未交付独立的数百本人工审校目录。

两种模式共用准入检查：默认排除教材、地方志、书目题记等专业记录，尊重明确类型，补取内容与作者资料，内容简介不足60字符不得拆封。分批检查最多48个候选、收集最多约16个可用候选；来源资料缓存7天，合格外部书池持久化最多500条。轻量模式不调用模型。

推敲评分下限70，至少引用10字符的内容原文；书名联想不能通过。探索同样受下限约束，第一批无匹配时比较第二批；仍无合适作品则失败退款。简介约束分字段引用来源，一次校验修正也计入服务端实际调用限制；不自动切换付费供应商。机器校验不等于完整事实审校。

实际验证：外部推敲抽到《远大前程》，简介、作者和阅读理由返回ready；首次选书约42秒、详情约6秒。普通版轻量抽到《神秘的奎因先生》，机会未扣。自动测试覆盖题记排除、无简介拒绝、标题联想拒绝、低分探索拒绝、轻量零生成、失败退款及错误引文修正。

## 通用版部署

通用版由 Sites Worker 提供 API，D1 持久化访客额度、书目缓存、详情和免费额度耗尽状态。每 8 小时 5 次，北京时间 00/08/16 点刷新，不累积。轻量抽取不限次数；同一 IP 每分钟最多 30 次非进度接口请求，全站同时处理一项抽书或详情任务，繁忙拒绝不扣机会。进度查询可并发。身份基于签名浏览器 Cookie，未提供账号跨设备身份；IP 限制用于抑制高频刷接口。

生产环境仅设置 DASHSCOPE_API_KEY 与两项 BAILIAN_*_FREE_QUOTA_CONFIRMED，不接收私用口令、无限版开关或本地代理。两款模型均需在百炼控制台开启免费额度用完即停；应用确认标记不能代替控制台开关。仅按白名单顺序切换，遇到 FreeTierOnly 后持久化停用该模型，两款都耗尽仍可随手抽。

构建 npm run build 输出 dist/client 与 dist/server/index.js。db/schema.ts 的变化通过 npm run db:generate 生成迁移。公开版本没有 /api/unlock，查询参数不能开启私用版，.env.local 和 .local-data 不打包。
